import React from "react"

const Breakdown = params => (
  <div>
    <div>{`<Form/> replaces <form onSubmit={formikProps.handleSubmit}>`}</div>
    <div>{``}</div>
  </div>
)

export default Breakdown
